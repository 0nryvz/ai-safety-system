# --- Model (Ultralytics YOLO, imgsz=640, 9 class) ---
AI_MODEL_PATH=./models/best.pt
AI_MODEL_VERSION=ppe-yolo26s-300ep-v1
AI_MODEL_DEVICE=cpu

# --- Inference thresholds ---
CONFIDENCE_THRESHOLD=0.50
WELDING_CONFIDENCE_THRESHOLD=0.25
NON_GLOVES_CONFIDENCE_THRESHOLD=0.40
IOU_THRESHOLD=0.45

# --- Backend (Spring Boot) ---
# AI Worker host makinede calisiyorsa:
BACKEND_BASE_URL=http://localhost:8080
#
# AI Worker Docker container icindeyse root docker-compose.yml
# host.docker.internal:8080 kullanir.
BACKEND_DETECTIONS_PATH=/internal/v1/detections

# Gercek ortamda Backend ile ayni Internal API Key kullanilmali.
INTERNAL_API_KEY=change-me-local-internal-key

BACKEND_REQUEST_TIMEOUT_SECONDS=2.0

# --- Class mapping ---
AI_CLASS_MAPPING_PATH=config/class_mapping.json