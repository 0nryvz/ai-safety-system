spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/veri_tabani_adin
    username: isg_user
    password: isg2026

  flyway:
    enabled: true
    locations: classpath:db/migration

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true